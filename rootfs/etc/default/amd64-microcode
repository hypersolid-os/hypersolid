# Configuration script for amd64-microcode version 3

# see /usr/share/initramfs-tools/hooks/amd64_microcode

# never install microcode updates to the early initramfs - use early to enforce
AMD64UCODE_INITRAMFS=no
